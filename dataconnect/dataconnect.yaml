specVersion: "v1"
serviceId: "newidea"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "newidea-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
